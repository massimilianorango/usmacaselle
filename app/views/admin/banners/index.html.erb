<% provide(:title, "Sponsors") %>
<ol class="breadcrumb">
  <li><%= link_to 'Home', admin_path %></li>
  <li class="active">Sponsors</li>
</ol>
<div class="page-container">
  <h2 class="page-header">Sponsors - banner presenti:
    <%= @h_banners.count + @v_banners.count %>
  </h2>
  <p class="alert alert-info">
    Vuoi
    <strong>ordinare</strong>
    i banner? Trascinali e l'ordine verrà registrato.
    <strong>Ricorda:</strong>
    l'ordine va da sinistra a destra, dall'alto al basso.</p>
  <p class="alert alert-warning"><strong>Attenzione!</strong>
    L'ordinamento non funziona se adblock o altri servizi per bloccare la pubblicità sono attivati sul proprio browser.</p>
  <div class="row">
    <div class="vertical_banners">
      <h3 class="page-header">Banner verticali:
        <%= @v_banners.count %></h3>
      <div class="banner-top-buttons">
        <%= link_to raw('<span class="icon"></span> Nuovo banner verticale'), new_vertical_new_admin_banner_path, :class => "btn btn-new" %>
      </div>
      <% if @v_banners.any? %>
        <div class="container-fluid">
          <div class="v_sortable row" style="text-align: center;">
            <% @v_banners.each do |v_banner| %>
              <%= render 'v_banner', v_banner: v_banner %>
            <% end %>
            
          </div>
        </div>
      <% end %>
    </div>
    <div class="horizontal_banners">
      <span class="controller">banners</span>
      <h3 class="page-header">Banner orizzontali: <%= @h_banners.count %> su 2</h3>
      <p>Il primo sarà posizionato in testa ad ogni pagina, il secondo in fondo.</p>
      <% if @h_banners.count < 2 %>
      <div class="banner-top-buttons">
        <%= link_to raw('<span class="icon"></span> Nuovo banner orizzontale'), new_horizontal_new_admin_banner_path, :class => "btn btn-new" %>
      </div>
      <% end %>
      <% if @h_banners.any? %>
        <div class="container-fluid">
          <div class="h_sortable row">
            <% @h_banners.each do |h_banner| %>
              <%= render 'h_banner', h_banner: h_banner %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
